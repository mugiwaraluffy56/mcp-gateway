apiVersion: v1
kind: Service
metadata:
  name: a2a-agent
  labels:
    app: a2a-agent
spec:
  ports:
  - name: http
    port: 8090
    targetPort: 8090
  selector:
    app: a2a-agent
  type: ClusterIP
